{
  "name": "todomvc-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx cypress run",
    "test-dev": "npx cypress open"
  },
  "devDependencies": {
    "cypress": "^4.12.1",
    "prettier": "^2.2.1"
  }
}
